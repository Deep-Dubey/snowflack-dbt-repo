sources:
  - name: "Dbt Demo"
    database: 'DB_ECOMM'
    schema: 'SC_ECOMM'
    tables:
      - name: 'USERS'
      - name: 'orders'

models:
  - name: "users_dbt"
    config:
      +materialized: 'table'
  - name: "demo_dbt"
    config:
      +materialized: 'view'
  # - name: "custv_4"
  #   config:
  #     materialized: 'ephemeral'
